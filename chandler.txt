

hpced
├── db.sqlite3
├── hpced
│   ├── asgi.py
│   ├── forms.py -- this has the descriptions of the forms
│   ├── __init__.py
│   ├── settings.py
│   ├── templates
│   │   └── hpced
│   │       ├── base.html -- this is the base template for the HTML, edits to the style should go here
│   │       ├── index.html -- This is the landing page 
│   │       └── metadata.html -- This is the form for adding new metadata entries
│   ├── urls.py -- This handles the routing from urls to views
│   ├── view.py -- This has the views, it handles the logic 
│   └── wsgi.py
└── manage.py

TO RUN
1. `cd` into the root of this repository
2. run `python3 -m venv venv`
3. run `source venv/bin/activate`
4. run `pip install -r requirements.txt`
5. `cd hpced`
6. `python3 manage.py makemigrations && python3 manage.py migrate`
7. `python3 manage.py runserver`
8. open localhost:8000 in a web browser
9. log in with Google